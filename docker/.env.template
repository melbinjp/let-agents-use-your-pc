# Jules Hardware Access - Docker Configuration Template
# Copy this file to .env and fill in your values

# ============================================
# REQUIRED: Cloudflare Tunnel Configuration
# ============================================

# Get your Cloudflare tunnel token from:
# https://one.dash.cloudflare.com/
# 
# Steps:
# 1. Go to Zero Trust > Networks > Tunnels
# 2. Create a new tunnel
# 3. Copy the token (starts with "eyJ...")
#
CLOUDFLARE_TOKEN=your_cloudflare_token_here

# ============================================
# REQUIRED: SSH Public Key
# ============================================

# Your SSH public key for Jules to connect
#
# Get your public key with:
#   Linux/Mac: cat ~/.ssh/id_rsa.pub
#   Windows: type %USERPROFILE%\.ssh\id_rsa.pub
#
# If you don't have an SSH key, generate one:
#   ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
#
# The key should start with "ssh-rsa" or "ssh-ed25519"
#
JULES_SSH_PUBLIC_KEY=ssh-rsa AAAA...your_public_key_here

# ============================================
# OPTIONAL: Advanced Configuration
# ============================================

# SSH port (default: 22)
SSH_PORT=22

# Jules username (default: jules)
JULES_USERNAME=jules

# Hardware name (for multiple hardware setups)
# HARDWARE_NAME=my-gpu-server

# ============================================
# NOTES
# ============================================

# Security:
# - Never commit this .env file to git!
# - Keep your private SSH key secure
# - Only share the public key
#
# Troubleshooting:
# - Ensure CLOUDFLARE_TOKEN is complete (very long string)
# - Ensure SSH key is the PUBLIC key (.pub file)
# - Check for extra spaces or line breaks
#
# Need help?
# - See: docker/QUICK_START.md
# - Run: python status.py
# - Check: TROUBLESHOOTING.md
